var globals = process.argv; // Get our Array of Variables
var total = 0;				// Set total to 0;

console.log(globals);

globals.forEach(function(value, index){
	console.log("Value", value);
	console.log("Index is: " + index);

	if (i != (0 || 1)) {
		
	}
});

